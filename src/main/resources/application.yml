spring:
  application:
    name: cube
  datasource:
    name: cube
    url: jdbc:mysql://192.168.1.104:9998?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: mayis001
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    root: debug
    com.futuredata.cloud.fdkpi.mapper: debug

server:
  port: 8888

